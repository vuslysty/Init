# Exercise 8
# What file contains the RSA keys that are authorized to connect via SSH?

# Answer:
# File which contains the RSA keys for authorization connection via SSH is
# in the folder ~/.ssh and has name "authorized_keys"
